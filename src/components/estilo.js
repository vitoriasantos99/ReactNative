import React from "react";
import { Text, View, StyleSheet } from "react-native";

export default estilogeral = StyleSheet.create(
    {
        fundo: {backgroundColor: '#1E90FF',},
        fonte: {fontSize: 50,},
    }
)
